#!/usr/bin/env python3
"""Executable wrapper: runs the fake-mirror scenario (design §8)."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))
import fake_mirror

sys.exit(fake_mirror.run("validate_valid"))
